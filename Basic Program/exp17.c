#include<stdio.h>
#include<conio.h>
#include<math.h>
int main()
{
    double x,sq;
    printf("Enter the values of x and y:");
    scanf("%lf",&x);
    sq=sqrt(x);
    printf(" %lf is sq of %lf",sq,x);
    return 0;
}
